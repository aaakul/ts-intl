---
title: "エラー処理とフォールバック"
description: "エラー処理とフォールバックの設定。"
order: 6
lang: "ja-JP"
---

# エラー処理とフォールバック

初期化時にエラーコールバックと未翻訳メッセージのフォールバック戦略を設定します：

```ts
import { createI18n, I18nError, I18nErrorCode } from "@aaakul/ts-intl";

export const i18n = createI18n({
  defaultLanguage: "ja-JP",
  messages: { "ja-JP": {} },
  onError(error: I18nError) {
    if (error.code === I18nErrorCode.MISSING_MESSAGE) {
      console.warn(
        `[ts-intl] 翻訳キー "${error.key}" が見つかりません（言語: ${error.lang}）`,
      );
    }
  },
  getMessageFallback({ key }) {
    return `[未翻訳: ${key}]`;
  },
});
```
